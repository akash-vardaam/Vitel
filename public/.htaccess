RewriteEngine On

# Allow direct access to existing files like submit.php before the SPA fallback.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send all other requests to React.
RewriteRule ^ index.html [L]
